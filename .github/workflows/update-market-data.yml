name: Update Market Data

on:
  schedule:
    # Runs at 9:30 PM UTC (5:30 PM ET) — Mon–Fri after market close
    - cron: '30 21 * * 1-5'
  workflow_dispatch:   # Allow manual trigger from GitHub Actions UI

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Fetch and update market data
        run: node scripts/update_data.mjs

      - name: Commit and push updated data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/al_index_data.json
          git diff --staged --quiet || git commit -m "data: auto-update $(date -u +%Y-%m-%d) [skip ci]"
          git push
